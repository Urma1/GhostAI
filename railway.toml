[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python3 bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Для постоянного хранения SQLite базы создайте Volume в Railway Dashboard
# и примонтируйте его к /data
